import { defineConfig } from 'dumi';

export default defineConfig({
  // entryFile:'./src/Switch/index.tsx',
  outputPath: 'docs-dist',
  themeConfig: {
    name: '@fe-rc/switch',
  },
  base: '/switch',
  publicPath: '/switch/',
});
